from classes.entityClass import entity

class node(entity):

    def __init__(self, ID, name, exp):
        skill.__init__(self, ID, name, exp)
        self.ID = ID
        self.name = name
        self.exp = exp
